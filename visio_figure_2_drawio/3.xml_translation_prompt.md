Task: Translate the flowchart text in the following draw.io XML code from English to Simplified Chinese.
Crucial Instructions:

Target Area: ONLY translate the text found inside the value="..." attributes of the <mxCell> tags.
Do NOT Break the Code: Do not change, translate, or remove any other XML tags, IDs, coordinates (x, y), or style attributes. Keep the XML structure perfectly intact.
Translation Rules:
Translate standard flowchart words (e.g., Start -> 开始, End -> 结束, Yes -> 是, No -> 否, Update -> 更新, Calculate -> 计算, Check if -> 检查).
KEEP all technical variables, mathematical parameters, and equation references exactly as they are in English.
Mathematical Formatting: You MUST format all English variables, mathematical parameters, and array names as inline math by wrapping them in \( and \). For example, value="Input: N, Dim" should become value="输入: \(N\), \(Dim\)" (or 输入: \(N, Dim\)), and Update X should become 更新 \(X\). For equation references, format them like Eq. \((9)\).
Output: Return the completely updated XML code inside a single code block so I can copy and paste it directly.