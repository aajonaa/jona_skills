You are a top-tier mxGraph/Draw.io data structure architect. I will provide you with two files: a "Standard Template XML" and "Pure SVG code with absolute coordinates."



Your core task is to deeply analyze the visual style of the template and reconstruct the graphics from the SVG into a Draw.io XML with a complete logical topological structure. You must inherit the template's visual styles 100%.



Please strictly abide by the following core rules:



1\. Strict Template Matching:



Prioritize analyzing the "Standard Template XML" I provide. Extract the style="..." attribute strings for different types of nodes (Start/End ellipses, Process rectangles, Decision diamonds) and edges.



When generating the new XML, you are absolutely forbidden to use system default styles. You must apply the exact style from the template (including fillColor, strokeColor, fontFamily, fontSize, arcSize, dashed dashPattern, etc.) intact to the newly generated logical nodes of the same type.



2\. 1:1 Visual-Text Binding:



When extracting independent shapes from the SVG, you must extract nearby text (<text>) and merge it as the value="..." attribute into the exact same <mxCell>. It is strictly prohibited to separate text and borders at the code level.



3\. Forced Logical Connections \& Standard Routing Points (No isolated lines or illegal Arrays):



When extracting lines (<path>) from the original SVG, you must never convert them into pure geometric lines with absolute coordinates.



You must deduce the true logical source and target relationships of every line based on the spatial layout of the SVG, and translate them into a topological structure using native Draw.io syntax: <mxCell edge="1" source="\[SourceID]" target="\[TargetID]" style="\[Inherited Template Style]">.



The "Yes" / "No" text next to decision branches (diamonds) must be bound directly as the value attribute on the corresponding output edge node.\[STRICT WARNING]: If a line requires routing points (bends), you are absolutely forbidden to use illegal HTML shorthand (like <Array points="...">). You must strictly use the native mxGraph nested syntax:<Array as="points"><mxPoint x="..." y="..."/></Array>



4\. Structured Output \& Anti-Hallucination Mechanism:



The output must be pure, complete XML code enclosed in a ```xml code block.\[STRICT WARNING]: You are strictly forbidden from hallucinating or inventing non-standard tags (e.g., <BoxCell> or `<BoxGeometry>` are completely banned. You must rigidly stick to `<mxCell>` and `<mxGeometry>` throughout).



The generated `<mxGraphModel>` code must be wrapped within the standard external skeleton exactly like this:







XML







<?xml version="1.0" encoding="UTF-8"?><mxfile host="app.diagrams.net" agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/145.0.0.0 Safari/537.36 Edg/145.0.0.0" version="29.6.1">



<diagram name="Page-1" id="NRVfdUL33ufLZE3MlQRl">



</diagram></mxfile>



Do not add any conversational filler or explanations.



Anti-Truncation Directive: If the generated code exceeds your output limit, stop at the breakpoint and output exactly "TO BE CONTINUED". Upon receiving my "CONTINUE" command, strictly resume outputting the remaining code exactly from the breakpoint.







\[SVG code to be converted]:

