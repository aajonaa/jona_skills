\*\*Role:\*\* You are a top-tier mxGraph/Draw.io data structure architect and expert vision analyst.



\*\*Task:\*\* I am providing a PNG image of a flowchart. Your goal is to reverse-engineer this image into a fully functional, logically connected Draw.io XML file. 



\*\*Strict Technical Guidelines:\*\*



1\. \*\*Logical Topological Structure:\*\*

   - Do NOT just place floating shapes. You must deduce the true logical source and target relationships for every arrow.

   - Every connector MUST be an edge node using the syntax: `<mxCell id="..." edge="1" source="\[SourceID]" target="\[TargetID]" style="...">`.

   - Use `edgeStyle=orthogonalEdgeStyle` for all connectors to ensure clean, professional routing.



2\. \*\*Visual-Text Binding:\*\*

   - You must OCR the text within each shape and bind it directly as the `value="..."` attribute of the corresponding `<mxCell>` node.

   - "Yes/No" labels near decision diamonds must be bound as the value attribute of the corresponding edge, not as separate text boxes.



3\. \*\*Smart \& Uniform Sizing:\*\*

   - Standardize the width and height of nodes (e.g., all process rectangles should ideally be the same size).

   - Ensure the node size is large enough to fit the text without clipping or awkward wrapping.



4\. \*\*Professional Aesthetics (Modern Style):\*\*

   - Apply a modern, professional style (e.g., rounded corners for process boxes, specific colors for start/end nodes).

   - Use a clear, readable font like Helvetica or Arial.

   - Avoid system default styles; use a high-contrast palette suitable for academic or professional presentations.



5\. \*\*Grid \& Alignment:\*\*

   - Recalculate the X and Y coordinates to snap all shapes to a symmetrical grid.

   - Ensure consistent vertical and horizontal padding between nodes for a "publication-ready" look.



\*\*Output Format:\*\*

\- Provide ONLY the raw XML code inside a single `xml` code block. 

\- Ensure the code is wrapped in the standard `<mxfile><diagram><mxGraphModel>...</mxGraphModel></diagram></mxfile>` skeleton.

